namespace AuthorizationGateway.DTOs
{
    public class LoginDTO
    {
        public string Username { get; set; } = string.Empty; // Initialized to avoid warnings
        public string Password { get; set; } = string.Empty; // Initialized to avoid warnings
    }
}
